# API Base URL Configuration
# Default: Nginx load balancer (port 8080)
app.api-base-url=http://localhost:8080/api

# TROUBLESHOOTING: If login fails with HTML error, try these alternatives:
# Option 1 - Direct connection to Server A (skip Nginx)
# app.api-base-url=http://localhost:8081/api

# Option 2 - Direct connection to Server B (skip Nginx)
# app.api-base-url=http://localhost:8082/api

# Make sure servers are running before starting the desktop client!